'use strict';
// You need to change the following environment variable name (COSMATT_TEST_ENVIRONMENT) and the corresponding value (stgCosmatt) based on your application
var env = process.env.CUP_APP_TEST_ENVIRONMENT || 'nemoProd1';
module.exports = env;

